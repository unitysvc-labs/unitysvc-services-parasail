name: Validate Data

on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - "services/**"
      - "platform_services/**"
      - ".github/workflows/validate-data.yml"
  push:
    branches: [main]
    paths:
      - "services/**"
      - "platform_services/**"
      - ".github/workflows/validate-data.yml"

jobs:
  validate-data:
    uses: unitysvc-labs/.github/.github/workflows/seller-validate-data.yml@main
